*.apk
.env
.env.local

# temporary build made when testing against a real API (see README)
dist-api

# product documentation - deliberately kept out of this public repo (70ec2cc),
# so a stray `git add -A` cannot put it back
*.docx
